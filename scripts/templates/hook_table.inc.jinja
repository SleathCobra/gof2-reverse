// hook_table.inc – AUTOGENERATED
// Do not edit. Use REGISTER_HOOK(address, function) to add your hooks.
#pragma once
#include <array>
#include <cstdint>

struct HookEntry {
    uintptr_t address;
    void* detour;  // set via REGISTER_HOOK
};

inline std::array<HookEntry, {{ functions | length }}> g_hookTable = {{
{%- for func in functions %}
    { {{ func.address }}, nullptr },  // raw: {{ func.raw_name }}{% if func.demangled %}, demangled: {{ func.demangled }}{% endif %}
{%- endfor %}
}};