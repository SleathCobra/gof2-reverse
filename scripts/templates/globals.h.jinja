// globals.h – AUTOGENERATED
// Default types guessed from name prefixes. Replace with actual types.
#pragma once
#include <cstdint>

{%- for global in globals %}
// Address: {{ global.address }}
inline constexpr uintptr_t ADDR_{{ global.name }} = {{ global.address }};
inline {{ global.default_type }}& {{ global.name }} = *reinterpret_cast<{{ global.default_type }}*>(ADDR_{{ global.name }});

{%- endfor %}